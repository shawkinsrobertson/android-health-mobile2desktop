# Add project specific ProGuard rules here.
# Release build has minification disabled by default (see app/build.gradle.kts),
# so this file is currently unused, but kept in place for when you enable it.
